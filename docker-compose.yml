services:
  sunadokei:
    build:
      context: .
      dockerfile: Dockerfile
    image: sunadokei:latest
    container_name: sunadokei
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - NODE_ENV=production
